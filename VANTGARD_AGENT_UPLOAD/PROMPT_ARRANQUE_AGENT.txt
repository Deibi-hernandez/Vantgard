Read and follow these files before making any changes:

1. .github/copilot-instructions.md
2. agent_docs/04_flujo_agente_nocturno.txt
3. agent_docs/05_segunda_version_agresiva_backend_first.txt

Then inspect the existing Django repository and work autonomously on a dedicated branch.

Main goal:
Strengthen the backend purchase flow of Vantgard without rebuilding the project from scratch.

Mandatory priorities:
- audit the real repository first
- improve the order domain
- strengthen cart and checkout flow
- add tracking token flow
- add automatic QR generation
- prepare a payment service abstraction using safe mock or sandbox-ready placeholders
- improve admin operations where useful

Important rules:
- do not recreate the project
- do not rename main apps, settings modules, or routes unless absolutely necessary
- keep changes incremental and reviewable
- prefer backend correctness over visual polish
- create migrations if models change
- document dependencies and setup notes
- if a task is too large, deliver a strong MVP and document the next step

Final requirement:
When finished, create a pull request with:
- clear summary
- files changed
- migrations
- dependencies
- local test steps
- limitations
- recommended next steps

Start by reading those files and summarizing the real repository structure before coding.
